#pragma once
// ============================================================================
// WiFi.h Mock — already defined in Arduino.h mock, this just prevents
// the real WiFi.h from being included.
// ============================================================================
#include "Arduino.h"
